//Did daily problem on leetcode for recurring addition with changing starting number after every 7 additions


class Solution {
    public int totalMoney(int n) {
        int money_on_monday=1,total=0,money_everyday=1;
    
        for(int i=1;i<=n;i++){
            if(i%7==0){
                total=total+money_everyday;
                money_on_monday+=1;
                money_everyday=money_on_monday;
            }
            else{
                total=total+money_everyday;
                money_everyday++;
            }   
        }
        return total;
    }
}

** This is not the final Code that I will publish here...there is a more optimised version that I will publish soon.

**UPDATE:- Just did the problem in a more optimised way... used a simpler n(n+1)/2 formula method.

class Solution {
    public int totalMoney(int n) {
        int minus=0,weeks=n/7,surplus=n%7,total=0,max=7;

        for(int i=0;i<weeks;i++){
            total=total+(max*(max+1))/2 - (i*(i+1))/2;
            max++;
            minus++;
        }

        if(weeks==0){
            total=total+(surplus*(surplus+1))/2;
        }

         else if(weeks!=0){
            total=total+((surplus+minus)*(surplus+minus+1))/2 - (minus*(minus+1))/2;
        }

        return total;
        
    }
}
